# Sanitized Google Apps Script export

This directory contains a sanitized export of the previous Google Apps Script backend.

Security rules:
- Do not commit real API keys.
- Do not commit `.clasp.json`.
- Runtime secrets must be stored in Cloudflare Secrets / server-side settings.
- This directory is an archive/migration source, not browser-side code.
